//Write a program that will print the maximum of two numbers whose
//values are read from the keyboard.

#include <iostream>
using namespace std;

int main() {
    int a,b,max;
    cin>>a>>b;

    if (a>b) {
        max=a;
    }else {
        max=b;
    }
    cout<<"Maximum: "<<max<<endl;
    return 0;
}

