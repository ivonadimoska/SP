/*Да се напише програма која вчитува од стандарден влез
 *два децимални броја (маса во кг и висина во цм) и пресметува
 *и печати на стандарден излез индекс на телесна маса по формулата:

BMI=masa/(visina*visina)

Висината претходно треба да се претвори од сантиметри во метри.
*/

#include <iostream>
using namespace std;

int main() {
    float kg, cm;
    cin>>kg>>cm;
    float m = cm/100;
    float BMI = kg/(m*m);

    cout<<"BMI: "<<BMI<<endl;
    return 0;
}
